{
  "name": "@apperside/app-ui",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b --incremental",
    "watch": "tsc -b --incremental --watch"
  },
  "dependencies": {
    "@apperside/ui-engine": "0.0.1",
    "react-native-web": "^0.12.2"
  },
  "devDependencies": {
    "node-sass": "^4.13.0"
  }
}
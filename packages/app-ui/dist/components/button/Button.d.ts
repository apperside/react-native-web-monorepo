import React from "react";
import { TouchableOpacity } from "react-native";
import { ButtonProps } from ".";
export declare const ButtonSpecs: import("styled-components").StyledComponent<typeof TouchableOpacity, import("styled-components").DefaultTheme, ButtonProps, never>;
declare const Button: React.FC<React.ComponentProps<typeof ButtonSpecs>>;
export default Button;
